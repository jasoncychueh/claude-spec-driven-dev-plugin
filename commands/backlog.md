---
description: "List, pick up, or close items in the unsorted backlog basket"
argument-hint: "[list | pick <id> | close <id> | drop <id>]"
---

Load the `spec-driven-development` SKILL, then run the `/backlog` flow.

Board command (the path is resolved here, not in the shell): `node "${CLAUDE_PLUGIN_ROOT}/scripts/board.mjs" backlog …`

Arguments: $ARGUMENTS
